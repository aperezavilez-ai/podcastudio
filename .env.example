# Clave de API de Anthropic (Claude) — solo servidor (Vercel + .env.local)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Resend — notificaciones por email (solo servidor)
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=PodcastStudio <notificaciones@podcaststudio.mx>

# Stripe — pagos de planes (solo servidor)
STRIPE_SECRET_KEY=sk_live_...
SITE_URL=https://podcaststudio.mx

# Supabase — públicas en el build (Vercel: redeploy tras añadirlas)
VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
